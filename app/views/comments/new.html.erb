<%= form_for([@article, @article.comments.new]) do |f| %> 
  <% if @article.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <h2><%= pluralize(@article.errors.count, "error") %>
prohibited this comment from being saved:</h2>
    <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
   
  <div>
    <button class="btn btn-default btn-sm field" role = "button"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> <%= 'Add'%></button>
  </div>

<% end %>